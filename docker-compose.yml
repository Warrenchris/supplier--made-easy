version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: supplier-made-easy
    ports:
      - "3000:3001"
    environment:
      - PORT=3001
      - NODE_ENV=production
    restart: unless-stopped
